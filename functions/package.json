{
  "name": "functions",
  "scripts": {
    "build": "tsc && node esbuild.mjs",
    "build:watch": "node esbuild.mjs --watch",
    "dev": "pnpm build:watch & firebase emulators:start --only functions,firestore --import ./emulators-data --export-on-exit ./emulators-data && fkill --silent :5002 :8081 :4011 :4500 :4400",
    "serve": "pnpm build && firebase emulators:start --only functions,firestore --import ./emulators-data --export-on-exit ./emulators-data && fkill --silent :5002 :8081 :4011 :4500 :4400",
    "shell": "pnpm build && firebase functions:shell",
    "start": "pnpm shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "16"
  },
  "type": "module",
  "main": "lib/index.js",
  "module": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^10.2.0",
    "firebase-functions": "^3.21.0"
  },
  "devDependencies": {
    "esbuild": "^0.14.50",
    "fkill-cli": "^7.1.0",
    "typescript": "^4.6.4"
  },
  "sideEffects": false,
  "private": true
}
